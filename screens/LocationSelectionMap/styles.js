import { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  map: {
    flex: 1,
    marginTop: 20,
    marginLeft: 20,
    marginRight: 20,
    alignItems: "center",
    justifyContent: "center"
  }
}));
