import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
      flex: 1,
      paddingTop: 0
    },
    listContainer: {
      flexGrow: 1,
      marginTop: -1
    }
  });